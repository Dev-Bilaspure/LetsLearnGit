#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int a,b;
    cout<<"Enter two number: ";
    cin>>a>>b;

    cout<<"Sum is: "<<a+b;
    return(0);
}